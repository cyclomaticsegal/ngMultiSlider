{
  "name": "ngMultiSlider",
  "version": "0.0.1",
  "description": "Slider Widget with support for (n) ranges",
  "main": "ngMultiPointSliderModule.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-exec": "^0.4.6"
  },
  "dependencies": {

  }
}
